import 'package:flutter/material.dart';


class PageCompriser extends StatelessWidget {
  const PageCompriser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}